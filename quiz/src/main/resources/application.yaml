server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/game
    username: "${username}"
    password: "${password}"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always